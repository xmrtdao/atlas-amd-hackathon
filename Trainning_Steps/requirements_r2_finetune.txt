# ============================================================
# ATLAS R2 FINETUNING STACK — Qwen3-30B on AMD MI300X
# FullFinetune + DeepSpeed + Flash-Attention + Optimized for ROCm 6.2
# ============================================================

# Core ML/DL Framework (ROCm 6.2 compatible)
torch==2.3.0
torchvision==0.18.0
torchaudio==2.3.0
pytorch-cuda==12.1

# Transformers & Hugging Face Ecosystem
transformers==4.40.2
huggingface-hub==0.22.2
tokenizers==0.15.1
datasets==2.18.0
evaluate==0.4.1
accelerate==0.27.2

# Advanced Training Optimizations
deepspeed==0.14.2
bitsandbytes==0.43.0
peft==0.10.0
flash-attn==2.5.8
triton==2.2.0
xformers==0.0.26

# Data Processing & Utilities
pandas==2.2.1
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2
tqdm==4.66.2

# Monitoring & Logging
wandb==0.16.6
tensorboard==2.16.2
loguru==0.7.2

# GPU/System Utilities
pynvml==11.5.0
psutil==5.9.8
py-cpuinfo==9.0.0

# JSON/Data Serialization
jsonlines==4.0.0
json5==0.9.14
pyyaml==6.0.1

# API & Web (for potential remote inference)
fastapi==0.109.2
uvicorn==0.27.0
pydantic==2.6.4

# CLI & Development
click==8.1.7
rich==13.7.0
python-dotenv==1.0.1

# Optional: LoRA for R3 (if needed later)
lora==0.2.0
loralib==0.1.1

# Optional: Quantization utilities
auto-gptq==0.7.1
optimum==1.17.1

# AMD ROCm specific (pre-installed on MI300X, but explicit)
rocm-torch==2.3.0

# ============================================================
# INSTALLATION NOTES:
# ============================================================
# 1. Use: pip install --break-system-packages -r requirements_r2_finetune.txt
# 2. Flash-Attention requires CUDA/ROCm headers (should be present on MI300X)
# 3. DeepSpeed will auto-compile CUDA extensions for ROCm
# 4. BitsAndBytes requires proper CUDA/ROCm runtime
# 5. If any build fails, check ROCm installation: rocm-smi
#
# EXPECTED INSTALLATION TIME: 15-25 minutes
# ============================================================
